import WebEditor from "@/components/common/WebEditor";

const RegisterItem = () => {
  return (
    <>
      <WebEditor editable={true} />
      <WebEditor editable={true} />
    </>
  );
};

export default RegisterItem;
